/**
 * AFK Preschool — App Configuration
 * 
 * HOW TO USE:
 * 1. Copy this file and rename it to config.js
 * 2. Replace the placeholder values with your actual AWS endpoints
 * 3. Make sure config.js is in .gitignore — never push real keys to GitHub!
 * 
 * config.js is loaded in index.html before main.js
 */

window.APP_CONFIG = {
  apiUrl: "YOUR_API_GATEWAY_URL_HERE"
  // Example: "https://xxxxxxxx.execute-api.ap-south-1.amazonaws.com/stage/register"
};
